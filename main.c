#include "shell.h"
/**
 * main - The entry point of the simple shell program.
 * This function serves as the entry point for the simple shell.
 * It calls the interactive_mode function,
 * which is responsible for handling user input and
 * executing commands in an interactive shell session.
 *
 * return - 0 on successful execution, indicating success.
 */
int main(void)
{
	interactive_mode();
	return (0);
}
